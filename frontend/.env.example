# Local dev can stay empty because vite.config.ts proxies /api and /auth to
# http://localhost:3000.
VITE_API_BASE_URL=""

# Production should point at the deployed Next.js backend, for example:
# VITE_API_BASE_URL="https://your-next-backend.vercel.app"
